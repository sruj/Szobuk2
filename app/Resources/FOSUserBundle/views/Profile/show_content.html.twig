{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <p class="greyTop">{{ 'profile.show.username'|trans }}</p><p class="rightP">{{ user.username }}</p>
    <p class="greyTop">{{ 'profile.show.email'|trans }}</p><p class="rightP" >{{ user.email }}</p> 
</div>
